/**
 * useAuth.js — Authentication context and hook.
 *
 * Provides:
 *   - AuthProvider context with login, logout, register functions
 *   - useAuth hook to access current user, JWT tokens, and auth state
 *   - Automatic token refresh on expiry
 */
