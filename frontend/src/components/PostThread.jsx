/**
 * PostThread.jsx — Recursive threaded post component.
 *
 * Renders a discussion post and its nested replies recursively.
 * Supports reply, edit, and delete actions based on user permissions.
 */
