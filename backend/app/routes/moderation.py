"""
routes/moderation.py — Moderation API endpoints.

Endpoints:
  DELETE /api/mod/posts/:id          — Remove a post (moderator+)
  POST   /api/mod/tracks/:id/pin     — Pin artist to a track
  POST   /api/mod/tracks/:id/delegate — Delegate moderation for a track
"""
