"""
routes/tracks.py — Track CRUD API endpoints.

Endpoints:
  GET    /api/tracks          — List all tracks (paginated)
  POST   /api/tracks          — Submit a new track (SoundCloud URL)
  GET    /api/tracks/:id      — Get a single track by ID
  PUT    /api/tracks/:id      — Update track details
  DELETE /api/tracks/:id      — Delete a track
"""
