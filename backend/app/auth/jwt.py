"""
auth/jwt.py — JWT token creation and verification.

Provides functions to:
  - Create access and refresh tokens with configurable expiry
  - Verify and decode JWT tokens
  - Extract user claims from tokens
"""
