"""
config.py — Application settings and environment configuration.

Uses pydantic BaseSettings to load configuration from environment
variables: database URL, JWT secret, token expiry, CORS origins,
debug mode, etc.
"""
